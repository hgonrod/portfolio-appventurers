---
interface Props {
  title: string;
  imageUrl: string;
  buttonText?: string;
  buttonLink?: string;
}
const { title, imageUrl, buttonText, buttonLink } = Astro.props;
---

<div class="row flex-lg-row-reverse align-items-center g-5 py-5">
  <div class="col-10 col-sm-8 col-lg-6">
    <img
      src={imageUrl}
      class="d-block mx-lg-auto img-fluid"
      alt="Flamante página de Astro"
      width="700"
      height="500"
      loading="lazy"
    />
  </div>
  <div class="col-lg-6">
    <h1 class="display-5 fw-bold lh-1 mb-3">{title}</h1>
    <slot>
      <p class="lead">
        Bienvenid@s a mi portfolio, desarrollado con el framework Astro. Soy un desarrollador web centrado en el mundo del back-end, buscando aumentar mis conocimientos del mundo del front-end. Ubicado en Santiago de Compostela, Galicia.
      </p>
    </slot>
    {
      buttonText && (
          <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href={buttonLink} class="btn btn-primary btn-lg px-4 me-md-2">
              {buttonText}
            </a>
          </div>
        )
    }
  </div>
</div>